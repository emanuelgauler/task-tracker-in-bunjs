{
  "name": "task-tracker",
  "module": "src/cli/index.js",
  "type": "module",
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "scripts": {
    "dev": "bun run src/cli/index.js",
    "setup": "bun build src/**/*.js --compile --outfile task-tracker"
  }
}